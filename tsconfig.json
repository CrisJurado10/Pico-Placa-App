{
  "compilerOptions": {
    /* --- Estructura de Archivos --- */
    "rootDir": "./",
    "outDir": "./dist",

    /* --- Configuración de Módulos (Crucial para Jest) --- */
    "module": "commonjs",     
    "target": "es2016",
    "esModuleInterop": true,
    
    /* --- LA SOLUCIÓN A TU ERROR --- */
    /* Aquí cargamos explícitamente los tipos de Node y Jest */
    "types": ["node", "jest"],

    /* --- Chequeos Estrictos (Clean Code) --- */
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "exactOptionalPropertyTypes": true,
    "forceConsistentCasingInFileNames": true,
    
    /* --- Otros --- */
    "skipLibCheck": true,
    "sourceMap": true,
    "declaration": true
  },
  /* --- Qué archivos debe leer TypeScript --- */
  "include": ["src/**/*", "tests/**/*"],
  "exclude": ["node_modules", "dist"]
}